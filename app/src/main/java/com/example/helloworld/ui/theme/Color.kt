package com.example.helloworld.ui.theme

import androidx.compose.ui.graphics.Color

// Colores de la paleta (manteniendo el morado en primary y usando tonos de azul)
val Purple40 = Color(0xFF6650A4)     // Color Morado (Primary)
val LightBlue = Color(0xFFBBDEFB)    // Azul Claro (Secondary en modo claro)
val DarkBlue = Color(0xFF0D47A1)     // Azul Oscuro (Tertiary en modo claro)
val BlueGray = Color(0xFF37474F)     // Gris Azul (Fondo y superficie en modo oscuro)
val LightGrayBlue = Color(0xFFECEFF1) // Gris Azul Claro (Fondo y superficie en modo claro)

// Colores adicionales para fondo y superficie en ambos temas
val LightBackground = LightGrayBlue
val DarkBackground = BlueGray
val LightSurface = LightGrayBlue
val DarkSurface = BlueGray

// Color de texto sobre fondo oscuro o claro
val OnPrimary = Color.White
val OnSecondary = Color.Black
val OnBackgroundLight = Color(0xFF1C1B1F)
val OnBackgroundDark = Color.White
